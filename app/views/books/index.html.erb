<h1>Books</h1>

<ul class="monsters">
  <% @books.each do |book| %>
    <h4><%= book.id %></h4>
    <h2><%= book.title %>,  by  <%= book.author %></h2>
    <h4>I have read this already: <%= book.already_read %></h4><br>
  <% end %>
</ul>

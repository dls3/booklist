<h1>Books</h1>

<ul class="monsters">
  <% @books.each do |book| %>
    <h1><%= book.title %>, by <%= book.author %></h1>
    <h4>I have read this already: <%= book.already_read %></h4>
  <% end %>
</ul>
